[package]
name = "anchor-proof"
edition = { workspace = true }
rust-version = { workspace = true }
version = { workspace = true }
publish = { workspace = true }

[dependencies]
anyhow = "1.0.104"
borsh = "1.8.1"
anchor-codec = { path = "../codec" }
anchor-commitment = { path = "../commitment" }
anchor-identity = { path = "../identity" }
jmt = { version = "0.12.0", default-features = false, features = ["std"] }
thiserror = "2.0.20"

[dev-dependencies]
ed25519-dalek = "3.0.0"
jmt = { version = "0.12.0", default-features = false, features = ["std", "mocks"] }
anchor-testing = { path = "../testing" }
